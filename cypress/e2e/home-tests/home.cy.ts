describe("Home module tests (auth)", () => {
  before("Mobile version", () => {
    cy.viewport("iphone-x");
  });

  describe("Accessing the home route without authorization"),
    () => {
      it("");
    };
});
